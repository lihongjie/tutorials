package tutorial;

public class Test5 {

    public static void main(String[] args) {

        String s = "Java";
        String n = "SE";

//        BiFunction<String, String, String> sf = (s1, n1) -> s1.concat(n1);
//        System.out.println(sf.apply(s, n)); // JavaSE

//        BiFunction<String, String> sf = (s1, n1) -> s1.concat(n1); // compilation fails
//        System.out.println(sf.apply(s, n)); //

//         Function<String, String> sf = (s1, n1) -> s1.concat(n1); // compilation fails
//         System.out.println(sf.apply(s, n));

//        Function<String, String, String> sf = (s1, n1) -> s1.concat(n1); // compilation fails
//        System.out.println(sf.apply(s, n));

    }
}
